import { z } from "zod";

export const adminSchema = z.object({
  //   firstName: z.string({ required_error: "field is required" }),
  //   middleName: z.string({ required_error: "field is required" }),
});
